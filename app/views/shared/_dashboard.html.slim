table
  thead
    tr
      th Name
      th Email
      th Status
      th Department

  tbody
    tr
    - @prospects.each do |prospect|
      tr
      = simple_form_for prospect do |f|
        td = prospect.name
        td = prospect.email
        td = f.association :status, prompt: "Current Status", label: false
        td = f.association :department, prompt: "Select Department", label: false
        td = f.button :submit
        td = link_to 'Show', prospect
        td = link_to 'Edit', edit_prospect_path(prospect)
        td = link_to 'Destroy', prospect, data: {:confirm => 'Are you sure?'}, :method => :delete
